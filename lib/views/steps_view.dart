import 'package:flutter/material.dart';

class StepsView extends StatelessWidget {
  const StepsView({super.key});
  static String stepsViewId = 'StepsView';
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
